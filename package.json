{
  "name": "blackline-deadfall",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "BLACKLINE: DEADFALL - a survival-horror zombie shooter in an infinite procedural deadland (three.js + Vite, every asset procedural).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "test": "node --test 'tests/*.test.mjs'",
    "smoke": "node tests/smoke.mjs"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "vite": "^8.3.0"
  }
}
