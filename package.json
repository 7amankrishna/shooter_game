{
  "name": "blackline-arena",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "BLACKLINE // ARENA - a polished 3D 1v1 player-vs-AI tactical shooter prototype (one map, one rifle, three difficulties).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "test": "node --test 'tests/*.test.mjs'"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "vite": "^8.3.0"
  }
}
